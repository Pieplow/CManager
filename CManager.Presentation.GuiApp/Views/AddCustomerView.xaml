<UserControl x:Class="CManager.Presentation.GuiApp.Views.AddCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CManager.Presentation.GuiApp.Views" 
             xmlns:viewmodels="clr-namespace:CManager.Presentation.GuiApp.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:AddCustomersViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Background="White" Margin="0,0,0,-191">
            
            <Label Content="Add a new customer" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,20"/>

            <Label Content="First name:" FontSize="16" Margin="0,10,0,5"/>
            <TextBox Text="{Binding NewCustomer.FirstName}"/>
            
            <Label Content="Last name: " FontSize="16" Margin="0,10,0,5"/>
            <TextBox Text="{Binding NewCustomer.LastName}"/>

            <Label Content="Email: " FontSize="16" Margin="0,10,0,5"/>
            <TextBox Text="{Binding NewCustomer.Email}"/>    

            <Label Content="PhoneNumber: " FontSize="16" Margin="0,10,0,5"/>
            <TextBox Text="{Binding NewCustomer.PhoneNumber}"/>
            
            <Label Content="Street Address: " FontSize="16" Margin="0,10,0,5"/>
            <TextBox Text="{Binding NewCustomer.Address.Street}"/>

            <Label Content="Postal Code: " FontSize="16" Margin="0,10,0,5"/>
            <TextBox Text="{Binding NewCustomer.Address.PostalCode}"/>    

            <Label Content="City: " FontSize="16" Margin="0,10,0,5"/>
            <TextBox Text="{Binding NewCustomer.Address.City}"/>

            <Button Content="Add" Command="{Binding AddCustomerCommand}" Margin="20, 20, 20, 2" Background="Green" />
            
            <Button Content="Back to menu" Command="{Binding BackToMenuCommand}" Margin="20, 20, 20, 2" Background="LightBlue" />

        </StackPanel>
    </Grid>
</UserControl>
